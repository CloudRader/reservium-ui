/**
 * Application Settings Configuration
 *
 * Central configuration file for app-wide settings like default values,
 * breakpoints, and pagination limits.
 */

export const APP_SETTINGS = {
  // Default reservation time range (24-hour format)
  DEFAULT_RESERVATION_START_TIME: 17,
  DEFAULT_RESERVATION_END_TIME: 21,

  // Responsive design breakpoint
  MOBILE_SCREEN_BREAKPOINT: 1024,

  // Pagination settings
  PAGINATION_LIMIT: 10,
};

